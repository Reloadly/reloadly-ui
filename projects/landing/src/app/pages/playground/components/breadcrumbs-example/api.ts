import { apiInterface } from '../../../../shared/components/api-table/models';

export const api: apiInterface[] = [
    {
        input: 'isActive',
        type: 'boolean',
        default: 'false',
        description: 'Specifies the active step'
    }
]
